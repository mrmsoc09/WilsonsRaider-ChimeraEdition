[pytest]
testpaths = tests
addopts = -q -ra
norecursedirs = .* build dist venv env .venv node_modules tools tools/* tools/osint tests/workflows
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
